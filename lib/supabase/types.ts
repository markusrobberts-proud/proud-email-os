/**
 * Placeholder for generated Supabase types.
 * Once the project is linked, regenerate via:
 *   pnpm dlx supabase gen types typescript --project-id iefqhohbyfjpeelrjfmc > lib/supabase/types.ts
 *
 * Until codegen runs, we use a permissive `any`-ish stand-in so the
 * Supabase JS SDK doesn't narrow query results to `never`.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
